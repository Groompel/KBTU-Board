export class Subject {
  code: string;
  name: string;
  faculty: string;

  constructor(code: string, name: string, faculty: string) {
    this.code = code;
    this.name = name;
    this.faculty = faculty;
  }
}
